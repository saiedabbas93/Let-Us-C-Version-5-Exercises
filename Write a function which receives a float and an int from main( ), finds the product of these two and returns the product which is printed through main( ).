#include<stdio.h>
#include<conio.h>
void main()
{
    float mul(int,float);
    int a=5;
    float b=2.5,c;
    c=mul(a,b);
    printf("Product is %f",c);
}
float mul(int a,float b)
{
    float c;
    c=a*b;
    return(c);
}

OUTPUT:
Product is 12.500000
